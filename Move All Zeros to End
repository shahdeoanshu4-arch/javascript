function moveZeros(arr) {
    let insertPos = 0;

    for (let num of arr) {
        if (num !== 0) {
            arr[insertPos++] = num;
        }
    }

    while (insertPos < arr.length) {
        arr[insertPos++] = 0;
    }

    return arr;
}

console.log(moveZeros([0,1,0,3,12]));
